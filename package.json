{
  "name": "flex-box",
  "version": "1.0.0",
  "description": "A github action allowing you to display (flex) your coding activity on a pinned gist (box) on your profile ",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drewbi/flex-box"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "Drew Alexander",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.5",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "dotenv": "^16.0.1",
    "eslint": "^8.0.0",
    "jest": "^27.2.5"
  }
}
